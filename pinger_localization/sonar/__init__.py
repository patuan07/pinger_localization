"""Pure numpy/scipy seed-traced sonar pipe detector (vendored).

Self-contained copy of the offline detector from the sonar_pipeline_experiment
repo (voter.py / trace.py / fit_segments.py), trimmed to the live node's needs
and importable without ROS. No runtime dependency on the experiment repo.
"""
